// LOG for creating schema

CREATE TABLE stock {
	_ID int AUTO_INCREMENT PRIMARY KEY,
	name varchar NOT NULL UNIQUE,
}

CREATE TABLE quantity {
	_ID int AUTO_INCREMENT PRIMARY KEY,
	stock_ID FOREIGN KEY REFERENCES stock(_ID),
	quantity int NOT NULL
}
//  so whats the application logic?

/*
	new product
		new row in product table
			key: val || name: [materialARR]
		new rows in stock table
			for each key, vals
				val: each, key: [0]
*/

/*
	add quantity ( name, quantity ) =>
		stock.name.push( quantity )
*/

/*
	new order ( { key: val } ) => 
		addQuantity( -1, thank_you, mailer )
		addQuantity( -val, key )
*/